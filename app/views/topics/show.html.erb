<!-- <p>
  <strong>User:</strong>
<%= User.find_by_id(@topic.user_id).email %> 
</p>

<p>
  <strong>Title:</strong>
  [<%= Tag.find_by_id(@topic.tag).name %>] <%= @topic.title %>
</p>

<p>
  <strong>Created:</strong>
  <%= time_ago_in_words(@topic.created_at) %> ago
</p>

<p>
  <strong>Content:</strong>
  </br>
  <%= @topic.content %>
</p> -->
<p>
<%= link_to 'Back', topics_path, :class=> 'btn btn-sm btn-default'%>
</p>
<p>
  <strong>
  <% if @topic.tag_id == 2 %>
          <span class = "label label-danger">
          <% elsif @topic.tag_id == 3 %>
          <span class = "label label-info">
          <% elsif @topic.tag_id == 4 %>
          <span class = "label label-success">
          <%end%>
          <%= Tag.find_by_id(@topic.tag).name%>
          </span>
          <%= @topic.title %></strong>
</p>
<p>
  by <%= User.find_by_id(@topic.user_id).email %> 
  created <%= time_ago_in_words(@topic.created_at) %> ago
</p>

<p>
  <strong>Content:</strong>
  </br>
  <%= @topic.content %>
</p>
<div  class = "btn-group">
<%= link_to 'Edit', edit_topic_path(@topic), :class=> 'btn btn-xs btn-default'%>
<%= link_to 'Delete', @topic, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-xs btn-danger'%>
</div>
<hr/> 

<%= render :partial => 'comment', :collection => @topic.comments %>

<%= form_for(@comment) do |f| %>

  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.hidden_field :topic_id %>

  <div class="field">
    <%= f.label "Comment" %><br>
    <%= f.text_area :body, :class => "form-control", :rows => "5"%>
  </div>
  <div class="actions">
  <%= f.submit "Create Comment", class: "btn btn-sm btn-success" %>
  </div>
<% end %>


